import Link from "next/link";
import { getCatalog } from "@/lib/data";
import { ProductActions } from "@/components/admin/product-actions";
import { money } from "@/lib/utils";
export default async function ProductsPage({searchParams}:{searchParams:{q?:string;stock?:string}}){const {products,categories}=await getCatalog();const q=(searchParams.q||"").toLowerCase(),rows=products.filter(p=>(!q||p.name.toLowerCase().includes(q))&&(!searchParams.stock||p.stock_status===searchParams.stock));return <><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Catalogue</p><h1 className="display mt-2 text-4xl">Produits</h1></div><Link href="/admin/products/new" className="button">Nouveau produit</Link></div><form className="mt-7 flex gap-3"><input name="q" defaultValue={searchParams.q} className="field" placeholder="Rechercher un produit"/><select name="stock" className="field max-w-48" defaultValue={searchParams.stock}><option value="">Tous les stocks</option><option value="in_stock">En stock</option><option value="on_order">Sur commande</option><option value="out_of_stock">Épuisé</option></select><button className="button">Filtrer</button></form><div className="card mt-5 overflow-x-auto p-2"><table className="w-full min-w-[720px] text-left text-sm"><thead className="text-xs uppercase text-ink/40"><tr><th className="p-3">Produit</th><th>Catégorie</th><th>Prix</th><th>Stock</th><th>Actions</th></tr></thead><tbody>{rows.map(p=><tr className="border-t border-ink/10" key={p.id}><td className="p-3 font-bold"><Link href={`/admin/products/${p.id}/edit`}>{p.name}</Link></td><td>{categories.find(c=>c.id===p.category_id)?.name||"—"}</td><td>{money(Number(p.price))}</td><td>{p.stock_status}</td><td><ProductActions id={p.id} active={p.is_active}/></td></tr>)}</tbody></table></div></>}
