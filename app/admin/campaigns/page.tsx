import Link from "next/link";import { getCatalog } from "@/lib/data";import { createClient,hasSupabaseEnv } from "@/lib/supabase/server";
export default async function Campaigns(){const {campaigns}=await getCatalog();let counts:Record<string,number>={};if(hasSupabaseEnv){const {data}=await createClient().from("cart_submissions").select("campaign_slug");(data||[]).forEach(x=>{if(x.campaign_slug)counts[x.campaign_slug]=(counts[x.campaign_slug]||0)+1})}return <><div className="flex items-end justify-between"><div><p className="eyebrow">Acquisition</p><h1 className="display mt-2 text-4xl">Campagnes</h1></div><Link href="/admin/campaigns/new" className="button">Nouvelle campagne</Link></div><div className="card mt-7 overflow-x-auto p-2"><table className="w-full min-w-[600px] text-left text-sm"><thead className="text-xs uppercase text-ink/40"><tr><th className="p-3">Nom</th><th>Slug</th><th>Lien</th><th>Demandes attribuées</th></tr></thead><tbody>{campaigns.map(c=><tr className="border-t border-ink/10" key={c.id}><td className="p-3 font-bold">{c.name}</td><td>?ref={c.slug}</td><td>{c.banner_link}</td><td>{counts[c.slug]||0}</td></tr>)}</tbody></table></div></>}
