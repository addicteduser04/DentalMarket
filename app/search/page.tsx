import { Search } from "lucide-react";
import { ProductCard } from "@/components/storefront/product-card";
import { getCatalog } from "@/lib/data";
export default async function SearchPage({searchParams}:{searchParams:{q?:string}}){const q=(searchParams.q||"").trim().toLowerCase();const {products,offers}=await getCatalog();const found=q?products.filter(p=>p.name.toLowerCase().includes(q)):products;return <div className="container-shell py-14"><p className="eyebrow">Catalogue complet</p><h1 className="display mt-3 text-5xl">Que recherchez-vous ?</h1><form className="relative mt-8 max-w-2xl"><Search className="absolute left-5 top-1/2 -translate-y-1/2 text-ink/35"/><input name="q" defaultValue={q} autoFocus className="field !rounded-full !py-4 !pl-14" placeholder="Miroir, composite, pince…"/></form><p className="mt-8 text-sm text-ink/50">{found.length} résultat{found.length!==1?"s":""}{q&&<> pour « {q} »</>}</p><div className="mt-6 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{found.map(p=><ProductCard key={p.id} product={p} offers={offers}/>)}</div></div>}
