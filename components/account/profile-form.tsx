"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export function ProfileForm({id,initial}:{id:string;initial:{full_name?:string|null;phone?:string|null;user_type?:string|null}}){const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const {error}=await createClient().from("profiles").update({full_name:f.get("full_name"),phone:f.get("phone"),user_type:f.get("user_type")}).eq("id",id);setMessage(error?.message||"Profil enregistré.")}return <form onSubmit={submit} className="card grid gap-5 p-7"><label className="text-sm font-bold">Nom complet<input name="full_name" className="field mt-2" defaultValue={initial.full_name||""}/></label><label className="text-sm font-bold">Téléphone<input name="phone" className="field mt-2" defaultValue={initial.phone||""}/></label><label className="text-sm font-bold">Je suis<select name="user_type" className="field mt-2" defaultValue={initial.user_type||"student"}><option value="student">Étudiant(e)</option><option value="professional">Professionnel(le)</option></select></label><button className="button">Enregistrer</button>{message&&<p className="rounded-xl bg-mint p-3 text-sm">{message}</p>}</form>}
